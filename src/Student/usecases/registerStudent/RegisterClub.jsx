// Components: Use Material UI (mui.com)
// Controller component
export default function RegisterStudentForm(){

    // R1. Content to be rendered (Rendering)
        // use react & react-router-dom
    // R2. State Management
        // context / Redux
    // R3. Hooks (managing Hooks)
        // Use react Hooks its within but the functions they call are in hooks.js
        useEffect(()=>{
            // this is a function from hooks.js
            // After importing it just call it here
        },[]);
    // R4. Presentation logic (Handling Presentation Logic)
        // use JS/ES6/TS
    // R5. API clients (Fetchers) (connecting 3rd party Apps)
        // Use axios

    return(
        <>
        <div>Register Student</div>
        </>
    )
}